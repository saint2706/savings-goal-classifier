# Pinned to the versions the published results were produced with.
# Lower bounds are the tested versions; upper bounds guard against the next
# major release changing behaviour the notebooks depend on.

pandas>=2.2.3,<3
numpy>=2.2.6,<3
scipy>=1.14.1,<2
matplotlib>=3.10.0,<4
seaborn>=0.13.2,<1
scikit-learn>=1.7.2,<2
xgboost>=3.1.1,<4

# SHAP is used only for `summary_plot`. SHAP <= 0.49 cannot parse XGBoost 3.x's
# base_score serialisation, so the notebooks compute SHAP values with XGBoost's
# own `pred_contribs=True` (the same TreeSHAP algorithm) rather than
# shap.TreeExplainer. See walkthrough/ihds_phase5.md.
shap>=0.49.1,<1

# Required to execute the notebooks non-interactively.
jupyter-core>=5.7
nbconvert>=7.16
nbformat>=5.10
ipykernel>=6.29
